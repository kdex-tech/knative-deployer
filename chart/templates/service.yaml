apiVersion: v1
kind: Service
metadata:
  name: {{ include "kdex-knative-faas-adaptor.fullname" . }}
  labels:
    {{- include "kdex-knative-faas-adaptor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kdex-knative-faas-adaptor.selectorLabels" . | nindent 4 }}
