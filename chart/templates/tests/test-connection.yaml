apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kdex-knative-faas-adaptor.fullname" . }}-test-connection"
  labels:
    {{- include "kdex-knative-faas-adaptor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kdex-knative-faas-adaptor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
